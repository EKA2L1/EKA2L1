#include <core/drivers/backend/ogl/shader_ogl.h>
#include <glad/glad.h>

namespace eka2l1 {
    namespace driver {
        bool shader_ogl::load(const char *raw_data, size_t data_size, const shader_type type) {
            return true;
        }

        bool shader_ogl::compile() {
            return true;
        }
    }
}